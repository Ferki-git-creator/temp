name: Build Android APK

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  # Дозволяє ручний запуск з інтерфейсу GitHub Actions
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      # Змінні середовища для P4A/Android
      ANDROID_NDK: "" # Очищаємо
      ANDROIDNDK: ""
      ANDROID_SDK_ROOT: "" 
      
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java (Temurin 17)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install apt dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            git wget unzip zip curl build-essential \
            python3-pip python3-venv \
            autoconf automake libtool pkg-config \
            zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 \
            cmake libffi-dev libssl-dev ccache ninja-build lld coreutils

      - name: Install python tools (cython, buildozer, p4a)
        run: |
          python3 -m pip install --upgrade pip setuptools wheel
          # Cython 0.29.33 - найбільш стабільна версія для Kivy
          python3 -m pip install cython==0.29.33
          # Фіксуємо Buildozer та P4A на перевірених версіях
          python3 -m pip uninstall python-for-android -y || true
          python3 -m pip install --user buildozer==1.4.0 
          # ВАЖЛИВО: Фіксуємо P4A на стабільній версії 2023.11.01, яка мала робочий рецепт libffi
          python3 -m pip install python-for-android==2023.11.01
          
          # додамо ~/.local/bin до PATH, щоб знайти команду buildozer
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Агресивне очищення кешу Buildozer/p4a
        run: |
          echo "Запускаємо АГРЕСИВНЕ ОЧИЩЕННЯ ВСІХ АРТЕФАКТІВ..."
          # ВИДАЛЯЄМО ВСЕ, включаючи старі збірки libffi, які можуть бути пошкоджені
          rm -rf ./.buildozer/android/platform/build-* || true
          rm -rf ~/.buildozer/android/packages || true
          rm -rf ./.buildozer/android/packages || true
          rm -rf ./.buildozer/android/platform/android-ndk-* || true
          rm -rf ~/.buildozer/android/platform/android-ndk-* || true
          rm -rf ./.buildozer/android/platform/android-sdk-* || true
          
      - name: Build APK using buildozer
        run: |
          echo "=== Starting APK build ==="
          
          export PATH="${HOME}/.local/bin:$PATH"

          # НЕ викликаємо update, оскільки P4A вже встановлено
          # python3 -m buildozer android update || true

          # Запуск збірки в режимі debug
          echo "y" | python3 -m buildozer -v android debug 2>&1 | tee build.log

          if ls bin/*.apk 1> /dev/null 2>&1; then
            echo "✅ APK successfully created!"
            ls -lh bin/*.apk
          else
            echo "❌ APK not found! Ось останні 200 рядків build.log:"
            tail -n 200 build.log || true
            exit 1
          fi

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: weather-app-apk
          path: bin/*.apk
          retention-days: 90

      - name: Upload build logs
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: build.log
          retention-days: 30

      - name: Optional:create GH release with APK
        if: ${{ github.event_name == 'push' && contains(github.ref, 'refs/tags/') }}
        uses: softprops/action-gh-release@v2
        with:
          files: bin/*.apk
          body: |
            # Weather App Release
            Android APK build from tag ${{ github.ref_name }}

